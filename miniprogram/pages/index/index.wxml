<view class="container">
  <view class="widthfull">
    <view class="card-list">
      <view class="card-item" wx:for="{{childPages}}" wx:key="page">
        <view data-page="{{item.page}}" bindtap="handerGoPage">{{item.name}}</view>
      </view>
    </view>
  </view>
</view>